# Local dev uses SQLite (zero install). For production, point this at a
# Postgres connection string instead — the schema is Postgres-portable.
DATABASE_URL="file:./dev.db"

# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AUTH_SECRET=""
